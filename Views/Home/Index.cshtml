@{
  Layout = "_Layout";
}

<h1>Keep Track of Your Contacts</h1>

@foreach(var contact in Model)
{
  <ul>
    <li>@contact.GetContactName()</li>
    <hr>
  </ul>
}

<a href="/add_contact"><button class="btn btn-default">Create New Contact</button></a>
